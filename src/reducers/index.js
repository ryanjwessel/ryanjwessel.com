const initialState = {
	portfolio: {
		skills: {},
		projects: {},
	}
};

const portfolioItems = (state = initialState, action) => {
	switch(action.type) {
		default:
			return state;
	}
}

export default portfolioItems;